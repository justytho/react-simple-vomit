//Import React and CSS for the footer
import React, { Component } from 'react';
import './Footer.css';

// define class Footer which extends Component
class Footer extends Component {
  render() {
//Render footer tag with className of footer
    return (
        <footer className="Footer">
        </footer>

    );
  }
}

//export footer component
export default Footer;
